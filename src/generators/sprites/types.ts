/**
 * Sprite Generator Types
 * =======================
 * Re-exports from central type definitions for easy imports.
 */

export type {
  SpriteConfig,
  SpriteResult,
  VideoSource,
  BaseGeneratorConfig,
} from '../../types/generators.js';

// Legacy compatibility
export type { SpriteInfo } from '../../types/index.js';
