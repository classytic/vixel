/**
 * Compression Generator Types
 * Re-exports from central types
 */

export type {
  CompressionConfig,
  CompressionResult,
  VideoCodec,
  HardwareAccel,
  EncodingPreset,
  AudioCodec,
  VideoSource,
  BaseGeneratorConfig,
} from '../../types/generators.js';
