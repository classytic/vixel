/**
 * Vixel primitives — the small set of orthogonal nouns the engine is built on.
 *
 * Currently: {@link Source} (input). Coming: Operation, Output, Profile.
 */

export { Source, type SourceKind, type SourceInit, type SourceJSON } from './source.js';
