{
  "name": "vixel-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Classytic vixel video stack — engine, schema contract, and editor UI as workspace packages.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "author": "Classytic",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "playwright-core": "^1.61.0"
  }
}
